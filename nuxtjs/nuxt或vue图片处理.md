# nuxt或vue图片处理

```
1. 图片的压缩裁剪 使用Compressor插件
2. 图片在定宽且定高的盒子中使用 css中的object-fit:cover处理
   object-fit: fill(强行撑满整个盒子) contain(以盒子为最大限度 按比例缩放) cover(按比例填充整个盒子 超出裁剪) none(保持原尺寸) 
   scale-down(none或contain选取一个 由对象尺寸小的决定)
```

